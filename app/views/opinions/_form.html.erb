

<%= form_for(@opinion) do |form| %>
  
  <%= form.label :content %>
  <%= form.text_field :opinion, id: :post_content, class: 'form-control', placeholder: 'Add New Post' %>
  <%= display_errors(@opinion) %>
  <%= form.submit 'Save', class: 'btn btn-primary' %>
<% end %>
